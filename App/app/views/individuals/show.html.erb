<%= render "layouts/header" %>

<div class="spacer" id="info">
        <div> 
                <span>Name: <%= @person.name %></span>
                <span>Age: <%= @person.age %></span>
                <span style="float:right"><%= link_to "delete account", @person, method: :delete %></span>
        </div>
        <%= link_to "Add equation", "#{@person.id}/new_equation" %>
</div>
<div id="equations">

<% if @person.equations.any?
        render 'layouts/equation_table', equations: @person.equations 
else %>
        <h2>No equations of this user</h2>
<% end %>

</div>

<%= link_to "Individuals", individuals_path %>
